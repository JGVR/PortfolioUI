import IFilter from "@/contracts/filter-interface";

export default function FilterDropDown({categoryFilters}: IFilter){
    return(
        <div className="flex flex-col gap-2 h-[25%] w-[10%] bg-black">
            
        </div>
    );
}
